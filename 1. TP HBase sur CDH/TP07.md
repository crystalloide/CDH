# TP07 : Utilisation de Sqoop, Pig et Hive avec HBase

## Présentation

Nous allons importer et exporter des données entre un SGBDR et HBase.

---

## Importation des données depuis HDFS vers HBase

### Présentation

**Durée :** 30 minutes

**Prérequis :** Vous devez avoir complété le TP06

---

### Tâches

**1. Importer le fichier `employes.csv` dans HDFS.**

a. Téléchargez l'exemple de fichier `employes.csv` fourni dans /donnees et sauvegardez-le sur votre machine locale.
employes.csv

text

b. Ouvrez l'invite de commande et, depuis le répertoire de téléchargement, chargez le fichier `employes.csv` dans HDFS :


c. Dans le terminal Cloudera Quickstart, exécutez la commande suivante pour copier le fichier `employes.csv` dans HDFS :

```bash
hdfs dfs -put /employes.csv /user/root
```

d. Dans le shell HBase, exécutez la commande suivante pour créer une table :

```hbase
create 'employes_donnees','cf1'
```

e. Dans le terminal Cloudera Quickstart, exécutez la commande suivante pour copier la table depuis HDFS vers HBase :

```bash
hbase org.apache.hadoop.hbase.mapreduce.ImportTsv \
  -Dimporttsv.separator=',' \
  -Dimporttsv.columns='HBASE_ROW_KEY,cf1:ename,cf1:designation,cf1:manager,cf1:date_embauche,cf1:sal,cf1:departementID' \
  employes_donnees /user/root/employes.csv
```

f. En cas de succès, vérifiez que toutes les données ont bien été écrites dans HBase.

g. Dans le shell HBase, exécutez la commande suivante pour afficher le contenu de la table :

```hbase
scan 'employes_donnees'
```

h. En cas de succès, vérifiez que le contenu de la table s'affiche dans HBase.

**2. Déconnectez-vous du shell HBase avec la commande `quit` :**

```hbase
quit
```

**3. Fermez les terminaux Hive, Pig et Cloudera Quickstart.**

---
